<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="trabalhopoo.nullapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconeapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".endereco"></activity>

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />

        <activity android:name=".MainActivity" />
        <activity
            android:name=".tela_comprar"
            android:label="@string/label_comprar" />
        <activity
            android:name=".tela_vender"
            android:label="@string/label_vender" />
        <activity
            android:name=".CadastrarCliente"
            android:label="@string/label_cadastracliente" />
        <activity
            android:name=".tela_editar"
            android:label="@string/label_editar" />
        <activity
            android:name=".tela_editar_produtos"
            android:label="Editar Produtos" />
        <activity
            android:name=".visualizarSacola"
            android:label="Visualizar Sacola" />
        <activity
            android:name=".sacolaCompras"
            android:label="Sacola" />
        <activity
            android:name=".CadastraCartao"
            android:label="Cartao" />
        <activity
            android:name=".tela_login"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>